# Infinite Canvas public SDK

`vendor/canvas-sdk-types.ts` and `vendor/LICENSE` are unmodified copies from
https://github.com/basketikun/infinite-canvas at commit
`d213a74614e0e4bd8a26383d1e1e907249e9c61b` (v0.18.0, SDK 0.1.0).

The types file SHA-256 is
`755010eeb2e53bcbbd96e02899d266b74ef177fcb6473466bdde05bad4c67257`.
The complete upstream MIT notice is in [vendor/LICENSE](vendor/LICENSE).
Only this public type contract is vendored. The plugin uses the documented
factory export and the host's React instance; it does not import host internals.
